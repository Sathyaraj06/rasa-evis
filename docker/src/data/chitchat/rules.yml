version: "3.1"

rules:
  - rule: respond to chitchat
    steps:
      - intent: chitchat
      - action: utter_chitchat

  - rule: Say 'I am a mock' anytime the user mocks
    steps:
      - intent: bot_mock
      - action: utter_mock
