version: "3.1"

rules:
  - rule: bot_who
    steps:
      - intent: chitchat
      - action: action_common

  - rule: Say 'I am a mock' anytime the user mocks
    steps:
      - intent: bot_mock
      - action: action_common
